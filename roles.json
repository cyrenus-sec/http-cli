{
    "roles": [
        {
            "name": "admin",
            "headers": {
                "Authorization": "Bearer admin-token",
                "X-Role": "admin"
            }
        },
        {
            "name": "guest",
            "headers": {
                "Authorization": "Bearer guest-token"
            }
        }
    ]
}
